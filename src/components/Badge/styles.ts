import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  top: 10px;
  right: -10px;

  width: 0.575rem;
  height: 0.575rem;
  background: rgb(255, 222, 51);
  border-radius: 1rem;
`;
